# Copy to .env and fill in. The skill reads these via `node --env-file=.env`,
# so the values never enter the chat. Never commit your real .env.

TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=
RETELL_API_KEY=key_...
